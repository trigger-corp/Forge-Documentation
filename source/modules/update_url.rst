.. _modules-update_url:

``update_url``: Automatic update urls
================================================================================

*Browsers only*.

URLs to check for application updates from.

Config
------

.. parsed-literal::
    {
        "modules": {
            "update_url": {
                "chrome": "url",
                "firefox": "url"
            }
        }
    }